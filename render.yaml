services:
  - type: web
    name: crypto-bot
    env: python
    buildCommand: pip install -r requirements.txt
    startCommand: python crypto_bot.py
    envVars:
      - key: PORT
        value: 10000
